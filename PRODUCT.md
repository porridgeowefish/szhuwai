# 产品说明：户外策划

## 产品定位

这是一个面向户外徒步用户的单任务工具：用户粘贴两步路线路 URL，系统负责打开真实授权页面、获取轨迹文件并生成可执行的户外策划。

## 核心用户

- 已在两步路发现目标线路，但不想手动寻找和下载 KML/GPX 的徒步用户。
- 希望快速获得轨迹难度、天气、交通、安全和装备建议的出行组织者。

## 核心流程

1. 粘贴两步路线路 URL。
2. 系统解析 `trackId`，打开独立浏览器授权窗口。
3. 用户扫码登录，并在出现验证码时手动完成；系统随后自动下载线路轨迹。
4. 用户可填写出行日期和出发地点；出发地点支持浏览器定位辅助填写。
5. 系统基于真实轨迹和已成功获取的数据生成策划。

## 产品原则

- 轨迹优先：首屏只服务于“从 URL 得到轨迹并生成策划”。
- 不捏造：未填写日期或未取得天气数据时，不生成天气结论；未填写出发地或未取得地图数据时，不生成交通结论。
- 隐私克制：API Key 仅保存在当前浏览器存储中，请求时发送给本地后端代理使用，服务端不持久化。
- 明确状态：打开窗口、扫码登录、人工验证、下载、分析、生成每一步都有可理解的状态和错误恢复方式。
- 无账户负担：不提供注册、短信、JWT、额度、个人资料、报告中心和后台管理。

## 品牌性格

可靠、冷静、清晰，像一张准备充分的行前检查表。视觉上保留户外感，但不模拟山景、不堆叠装饰，不做泛 AI SaaS 营销页。

## 反例

- 禁止玻璃拟态、彩色渐变、巨大圆角、装饰性山脉 SVG 和无意义动效。
- 禁止用多个卡片重复解释同一件事。
- 禁止把 API 配置、技术名词或次要工具放在主流程之前。
- 禁止用“已授权”“已下载”等文案掩盖尚未验证的外部状态。
